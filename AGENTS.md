# Agent guidance
For static building rigging, FBX/GR2 conversion, texture export and animation/material XML, read `.claude/skills/aoe-building-pipeline/SKILL.md`. Existing specialized skills remain in `.claude/skills/`; use the relevant one for the task.
Generic architectural authoring sources live in the user's separate architecture-blender-skills repository. Keep source assets out of runtime folders. Preserve unrelated changes. Keep art and `_snds` XML editable; do not introduce XMB copies for them.

[pytest]
markers =
    slow: full-map mapcheck runs (may need the game install; skip with -m "not slow")
    local(*needs): reads the owner's machine - "steam" = the AoE3DE install, "profile" = the profile folder (default both); skipped where that folder is missing, see conftest.py (CI runs -m "not local")
